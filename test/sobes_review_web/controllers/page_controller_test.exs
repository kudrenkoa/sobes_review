defmodule SobesReviewWeb.PageControllerTest do
  use SobesReviewWeb.ConnCase, async: true

  # test "GET /", %{conn: conn} do
  #   conn = get(conn, "/")
  #   assert html_response(conn, 200) =~ "Review App"
  #   IO.inspect(html_response(conn, 200))
  # end
end
